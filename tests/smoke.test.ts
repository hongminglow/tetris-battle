/**
 * Placeholder smoke test — proves vitest is wired correctly.
 * Replaced/augmented in Task 2 by board.test.ts / kicks.test.ts / piece.test.ts.
 */
import { describe, it, expect } from "vitest";

describe("smoke", () => {
  it("runs vitest", () => {
    expect(1 + 1).toBe(2);
  });
});
